<%= form_with(model: genre_dependency, local: true) do |form| %>
  <% if genre_dependency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genre_dependency.errors.count, "error") %> prohibited this genre_dependency from being saved:</h2>

      <ul>
      <% genre_dependency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :genre_id %>
    <%= form.number_field :genre_id %>
  </div>

  <div class="field">
    <%= form.label :super_genre_id %>
    <%= form.number_field :super_genre_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
