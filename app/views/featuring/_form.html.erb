<%= form_with(model: featuring, local: true) do |form| %>
  <% if featuring.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(featuring.errors.count, "error") %> prohibited this featuring from being saved:</h2>

      <ul>
      <% featuring.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.number_field :artist_id %>
  </div>

  <div class="field">
    <%= form.label :song_id %>
    <%= form.number_field :song_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
