<%= form_with(model: production_type, local: true) do |form| %>
  <% if production_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(production_type.errors.count, "error") %> prohibited this production_type from being saved:</h2>

      <ul>
      <% production_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :production_type_id %>
    <%= form.text_field :production_type_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :super_type %>
    <%= form.text_field :super_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
